# 项目永久规则

## 1. 代码提交方式

- **禁止使用 Pull Request 方式提交代码**
- 所有代码修改直接以本人身份（BossforAlex）提交到 `main` 分支
- 使用 `git push origin HEAD:main` 直接推送
- 如需密钥或 Token，直接向本人请求，不允许自行生成或猜测

## 2. GitHub 分支管理

- **仅保留 `main` 分支**
- 删除所有其他 branches（包括远程和本地）
- 所有开发工作直接在 `main` 分支上进行
- 不创建 feature 分支、develop 分支或其他任何分支